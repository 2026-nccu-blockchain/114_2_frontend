/**
 * Example Test File
 * This shows how to structure tests in the project
 */

describe('Example Test Suite', () => {
  it('should demonstrate basic test structure', () => {
    expect(true).toBe(true)
  })

  it('should test utility functions', () => {
    const result = 1 + 1
    expect(result).toBe(2)
  })
})
